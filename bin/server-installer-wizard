#!/usr/bin/env bash
# server-installer-wizard — interactive / unattended frontend for the CLI.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec bash "${ROOT}/wizard/main.sh" "$@"
