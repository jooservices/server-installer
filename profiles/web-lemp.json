{
  "profile_name": "web-lemp",
  "description": "LEMP: Nginx + PHP-FPM 8.5 + MySQL (Docker). Set SI_MYSQL_ROOT_PASSWORD in CI/secrets.",
  "modules": [
    "packages",
    "docker",
    "docker_group",
    "nginx",
    "php",
    "mysql"
  ],
  "env": {
    "SI_PHP_VERSION": "8.5",
    "SI_PHP_MODE": "fpm"
  }
}
